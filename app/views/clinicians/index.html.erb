<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
	 <div class="container">
		 <div class="navbar-header">
			 <%= link_to "Private Clinicians in your area", clinicians_path, { :class=>"navbar-brand" } %>
		 </div>
	 </div>
 </nav>

 <div class="container sparse-page">
	 <div class="list-group">
		 <div>
		 	<% @clinicians.each do |c| %>
			<div class='jumbotron'>
					<div class='.col-md-4'> <%= link_to c.name, clinician_path(c) %> <%= c.specialism %> <%= c.location %>  </div>
				</div>
			<% end %>
	 	</div>
	 </div>
 </div>
